module.exports = {
    siteUrl: 'https://yourdomain.com', // replace with your real URL
    generateRobotsTxt: true, // (optional) creates robots.txt
    // optional: exclude certain pages
    // exclude: ['/secret-page'],
};
